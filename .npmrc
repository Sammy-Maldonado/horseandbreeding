# Prisma resolves .prisma/client by walking up from its own location, which
# breaks under pnpm's isolated store: the generated client lands outside the
# path the runtime searches. A hoisted layout keeps resolution deterministic.
# Install-script blocking (see pnpm-workspace.yaml) is unaffected by this.
node-linker=hoisted
