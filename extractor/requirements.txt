# Horse & Breeder — Word extractor dependencies
#
# Only python-docx is required. Everything else the extractor uses
# (sys, re, json) is Python standard library.
#
# Version pinned to what was verified locally on Python 3.14.5.
python-docx==1.2.0
